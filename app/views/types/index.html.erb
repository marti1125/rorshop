<h1>Listing posts</h1>
<%= link_to 'New Type', :action => :new %>
<br/><br/>
<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>  
  </tr>

  <% @types.each do |type| %>
    <tr>
      <td><%= type.name %></td>
      <td><%= link_to 'Edit', :action => :edit, :id => type.id %></td>
      <td><%= link_to 'Delete', { :action => :destroy, :id => type.id }, :method => :delete, :data => { :confirm => 'Are you sure?'} %></td>
    </tr>
  <% end %>
</table>